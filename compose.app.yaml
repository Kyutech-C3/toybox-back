services:
  app:
    build:
      dockerfile: ./docker/app.dockerfile
    env_file:
      - .env
    volumes: !reset []
    dns: !reset []
    depends_on: !reset
      db:
        condition: service_healthy

  localstack-s3:
    profiles:
      - disabled-by-app
